---
import '../styles/global.css';
import Nav from '../components/Nav.astro';
import { withBase } from '../lib/paths';

interface Props {
  /** Used for previews (Open Graph); the browser tab always shows the site name. */
  title: string;
  description?: string;
}
const { title: pageTitle, description = 'ML/AI Engineer — Vidit Ostwal' } = Astro.props;

const tabTitle = 'Vidit-Ostwal';

/** Absolute icon URL for production (Safari / some clients ignore relative SVG); falls back to base path in dev. */
function iconUrl(rel: string): string {
  const path = rel.startsWith('/') ? rel.slice(1) : rel;
  const siteHref = Astro.site?.href;
  if (siteHref) {
    const base = siteHref.endsWith('/') ? siteHref : `${siteHref}/`;
    return new URL(path, base).href;
  }
  return withBase(`/${path}`);
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <title>{tabTitle}</title>
    <link rel="icon" type="image/svg+xml" href={iconUrl('favicon.svg')} sizes="any" />
    <link rel="icon" type="image/png" sizes="32x32" href={iconUrl('favicon-32.png')} />
    <link rel="icon" type="image/png" sizes="48x48" href={iconUrl('favicon-48.png')} />
    <link rel="shortcut icon" href={iconUrl('favicon.ico')} />
    <link rel="apple-touch-icon" sizes="180x180" href={iconUrl('apple-touch-icon.png')} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:wght@400;500;600&family=Lora:ital,wght@0,500;1,400;1,500&display=swap" rel="stylesheet" />
  </head>
  <body>
    <Nav />
    <main>
      <slot />
    </main>
    <footer>
      <div class="wrap">
        <span>© {new Date().getFullYear()} Vidit Ostwal</span>
      </div>
    </footer>
    <script>
      const io = new IntersectionObserver(
        (es) => es.forEach((e) => e.isIntersecting && e.target.classList.add('visible')),
        { threshold: 0.06 }
      );
      document.querySelectorAll('.fade-in').forEach((el) => io.observe(el));
    </script>
  </body>
</html>

<style>
  footer {
    border-top: 1px solid var(--border);
    padding: 2rem 0;
    margin-top: 3rem;
  }
  .wrap {
    max-width: 320px;
    margin: 0 auto;
    padding: 0 1.5rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.72rem;
    color: var(--text-3);
  }
</style>
